export type Author = {
  id: string;
  name: string;
  description: string;
};

export type Book = {
  isbn: string;
  title: string;
  author: string;
  price: string;
  thumbnailUrl: string;
  productDescription: string;
};
